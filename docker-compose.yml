services:
  web:
    build: .
    restart: unless-stopped
    networks:
      - default
      - caddy
    labels:
      caddy: mcapi.cf12.org
      caddy.reverse_proxy: "{{upstreams 8080}}"

  redis:
    networks:
      - default
    image: "redis:alpine"

networks:
  caddy:
    external: true
